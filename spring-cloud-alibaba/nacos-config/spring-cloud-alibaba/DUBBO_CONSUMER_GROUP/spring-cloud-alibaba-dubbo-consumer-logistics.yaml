spring:
  application:
    name: spring-cloud-alibaba-dubbo-consumer-logistics
  mvc:
    throw-exception-if-no-handler-found: true  # 使用 SpringBoot 捕获 404 异常
  web:
    resources:
      add-mappings: true  # 禁止静态资源的映射，加载自定义 404 异常

dubbo:
  application:
    qos-port: 11250

server:
  port: 1125

logging:
  config:
    - classpath:log4j2.xml